@Library('jenkins-pipeline-workshop-library') _

node {
    utilB()
}

pipeline {
    agent any

    stages {
        stage ('Testing') {
            steps{
                echo "Just testing"
            }
        }
    }
}